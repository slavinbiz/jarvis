# 2026-09-15

## Сессия 1 (автопубликация практики)

### Сделано
- Опубликован пост в @santex_s_ai (практика, автопуск 12:00): «Три раза чинил один лабиринт, и каждый раз ошибка была не там, где искал» — со своим фото Вячеслава (прислал заранее, генерация через Pollinations не понадобилась)

## Сессия 2 — докрутили обрыв сессии 12.09 (крипто-бот)

### Сделано
- Вячеслав вчера сказал, что сессию прервало токенами. Проверил crypto_bot: 2 локальных коммита от 12.09 23:51-23:57 (вход по лимитке на open) были ЗАДЕПЛОЕНЫ на TimeWeb, но не запушены на GitHub — `git push origin main` (`ffcf5b2..5a0443c`)
- Проверил хэш `/root/crypto_bot.py` на сервере — совпадает с локальным, `crypto-bot.service` активен. Деплой был сделан, только пуш забыли

## Сессия 3 — «Трафик Барановой»: урок 18, «Вырежи и собери» (пазл из кусочков)

### Сделано
- Первая попытка не туда: построил `copy-picture.html` (редактор клеток + пустая копия для срисовки) — Вячеслав показал скриншот реального референса (Scriptstock): формат другой, «вырежи и собери» — картинка режется на неправильные куски-пятна, лист = эталон + пустой контур-силуэт + кусочки с пунктиром на отрез
- Уточнил у Вячеслава источник картинки: генерируется во внешнем ИИ заранее, в инструмент просто загружается файлом (не рисуется руками)
- Задача сложнее предыдущих форматов (пикселизация загруженного файла, флуд-филл фона, сидированная нарезка на связные куски, раскладка потоком) — сразу составил подробный промпт для DeepSeek: `трафик барановой/2 недея/deepseek-task-puzzle-cut.md`

### TODO
- Ждём результат от DeepSeek — разобрать, проверить playwright-скриншотами (как cryptogram.html), поправить баги
- Решить, удалять ли неподошедший `copy-picture.html` (пока оставлен, не трогать без ОК)

## Сессия 4 — урок 18: DeepSeek принёс код, нашёл и починил баг

### Сделано
- Вячеслав прогнал промпт через DeepSeek — файл `18/решение.txt` пришёл целиком (без обрыва)
- При разборе нашёл баг до тестов: в `redraw()` черновая оценка высоты для кусочков считалась ДО добавления высоты заголовка/подзаголовка, а реальная раскладка — после — оценка почти всегда занижена. Ветка «пересоздать canvas» при расхождении сама вызывала `redraw()` рекурсивно с той же (неизменной) неверной оценкой
- Подтвердил живьём: playwright зависал на `page.screenshot` (таймаут 30с) после загрузки картинки — точно бесконечная рекурсия/подвисание вкладки
- Переписал `redraw()` в один проход: все позиции (заголовок → эталон → контур → кусочки) считаются заранее по формулам, canvas создаётся один раз, без рекурсии и без "пересоздания". Сохранил как `трафик барановой/2 недея/puzzle-cut.html`
- Проверил playwright: непрозрачная картинка (крест, белый фон → фон вырезан флуд-филлом), прозрачная PNG (ромб с кругом, альфа-канал → силуэт по альфе), пустой заголовок/подзаголовок (раскладка не ломается), смена размера клетки и диапазона кусков, повторная нарезка (seed меняется, форма кусков другая), скачивание PNG (41.8 КБ, без ошибок). Ошибок в консоли нет во всех сценариях

### Итог
Урок 18 «Скопируй картинку» → на деле «Вырежи и собери» (пазл из кусков) готов и рабочий: `трафик барановой/2 недея/puzzle-cut.html`. DeepSeek на этот раз не оборвался (задача была явно сложнее криптограммы), но принёс рабочий скелет с одним логическим багом — нашёл и поправил сам, не пересобирая с нуля

### TODO на потом
- Следующий формат — урок 19 «I Spy»
- Неподошедший `copy-picture.html` (первая, ошибочная попытка урока 18) остаётся в папке, не трогать без ОК Вячеслава

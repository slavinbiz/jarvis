# 2026-07-19

## Сессия 1

### Сделано
- Закрыл TODO с 13.07: переговоры с личным лидом @brandiumsu ничем не закончились
- Крипто-бот: нужен был анализ сигналов — выяснилось, что исходы проверок (🟢/🔴/⚪ через 15/60/240 мин) нигде не сохранялись, только уходили в Telegram сообщением. Добавил `init_signals_db()`/`save_signal()`/`save_signal_check()` в `crypto_bot.py` — SQLite `/root/signals.db` на сервере, таблицы `signals` (символ, направление, цена входа, время, тренд) и `signal_checks` (id сигнала, минуты, %, вердикт). 3 новых теста + все 22 старых зелёные. Задеплоено на TimeWeb (scp + `systemctl restart crypto-bot`), база создаётся и пишется, бот стартовал чисто
- Идея была сделать Jarvis-бота админом в чате сигналов ради истории — отклонил: Bot API физически не даёт ботам читать историю чата даже с правами админа, это протокольное ограничение, не вопрос прав
- Вячеслав экспортировал историю чата (`ChatExport_2026-07-19`, HTML). Разобрал: 902 сигнала за 18.05–19.07 (556 Pump / 346 Dump), но проверок с исходом только 93 (функция появилась 13.07 + баг с потерей asyncio-таски до фикса съедал часть). По горизонтам: 15м 37%🟢/44%🔴, 60м 47%🟢/44%🔴 (почти монетка), 240м 34%🟢/61%🔴 (чаще разворот на длинной дистанции). Топ по частоте: EDEN (126), POND (87), ALLO (86), RIF (50), HMSTR (44), FIDA (43)
- Предложил автоблок пар с явно отрицательным исходом (напр. BANK) — отклонено, SQLite копим только для анализа, без автоматических решений по чёрному списку
- Lead Machine на Beget: очередь (`queue`) пустая, 0 записей — не копится. Один диалог (лид #7, contacted) в стадии "conversation" с 08.07, ничего не висит на подтверждении. Поиск лидов работает без ошибок в логах, но реально почти не находит новых — последние циклы: 1 кандидат за раз, отфильтрован как нерелевантный (`t.me/profiwork`), 0 новых лидов сохранено. leads по статусам: new 3, rejected 3, contacted 1
- SSH-ключ `jarvis-vps` на Beget пропал из `/root/.ssh/authorized_keys` — там остался только ключ `beget-access-key` (от 27.05, положен панелью cp.beget.com). Похоже, панель Beget при повторной генерации root-доступа перезаписывает файл целиком, стирая вручную добавленные ключи. Вячеслав дал пароль root (через plink, пароль не храню в файлах), дописал наш ключ обратно — доступ по ключу восстановлен и проверен

### Решения
- Если SSH-ключ на Beget снова пропадёт — вероятная причина та же (перегенерация доступа в панели cp.beget.com затирает `authorized_keys`), не искать взлом. Чинится тем же способом: пароль от Вячеслава → plink → дописать ключ обратно, не перезаписывая существующую запись
- Историю сигналов до сегодня решили не пытаться восстановить через SQL — Вячеслав вручную экспортирует историю чата из Telegram Desktop (Export chat history), если понадобится анализ старых сигналов
- Новую историю с 19.07 накапливаем через SQLite на сервере — через пару недель будет на чём анализировать
- Основной VPN (Hysteria2-туннель) не трогаем — работает нормально. Речь только про запасной вариант на случай если он ляжет
- ProtonVPN Plus Stealth в этой роли не подошёл — проверено с компьютера и с телефона, блокировку не пробивает нигде. Ищем другой запасной вариант

### TODO
- [ ] Подобрать новый запасной VPN для компьютера взамен ProtonVPN Stealth

# 2026-07-01

## Сессия 1

### Сделано
- Старт сессии, загружен контекст
- Починен личный VPN-туннель (Hysteria2 через s-ui на TimeWeb 64.188.57.249): причина — мусорная запись клиента `test-trigger` (id=2, config=NULL, осталась с сессии 25 июня) ломала сборку конфига ядра, из-за чего Hysteria2-core падал в бесконечный цикл рестарта. Не работал VPN у всех: у меня и у обоих клиентов бота. Отключил запись (`enable=0`, DELETE заблокирован в settings.json), рестарт s-ui — ядро поднялось на UDP/443, трафик подтверждён curl'ом
- Обнаружено: на машине есть SSH-ключ `~/.ssh/id_ed25519`, даёт прямой доступ root на 64.188.57.249 и 91.193.25.237 без пароля — не нужен plink/пароль, как считалось раньше

- Починен Jarvis-бот на Beget (91.193.25.237): OAuth-токен Claude Code истёк 28 июня и не обновился автоматически — бот на каждое сообщение спамил "Failed to authenticate 401" 3 дня, из-за чего ручной refresh через console.anthropic.com/v1/oauth/token попадал под rate limit (429), долго ждали, не помогло. Решение — полный релогин: `claude auth login --claudeai` в persistent-сессии (`tmux`/`systemd-run --uid=1000`, чтобы сессия не умирала при закрытии SSH), Вячеслав открыл выданную ссылку, дал код авторизации, вставили через `tmux send-keys` → токен обновлён, бот перезапущен, отвечает
- Попутно включил `loginctl enable-linger agent` на Beget — без этого фоновые процессы (tmux/screen), запущенные через `sudo -u agent` внутри root-сессии SSH, умирали при разрыве соединения
- Вячеслав подтвердил: бот отвечает, проблема закрыта

### Решения
- Тестовые записи в s-ui.db (созданные при дебаге триггеров/багов) — не оставлять в базе, они могут молча ломать сборку конфига ядра. Проверять `SELECT * FROM clients` на NULL config после экспериментов
- Если OAuth-токен Claude Code на сервере истекает и не обновляется сам — не долбить refresh endpoint повторно (быстро попадает под rate limit), сразу делать полный `claude auth login` через tmux
- Не спрашивать подтверждение на действия вне красной зоны (деньги/безопасность/необратимость) — Вячеслав явно попросил дважды за сессию

## Сессия 2

### Сделано
- Починена s-ui панель: клиенты не отображались из-за ошибки `unsupported Scan type string into *json.RawMessage`. Причина: sui.py вставлял поле `inbounds` как TEXT, а s-ui ожидает BLOB. Исправлено: `CAST(inbounds AS BLOB)` для существующих записей + `.encode()` в sui.py для новых
- Починен клиент `vpnbot_6124820268` (produser_viacheslav): был отключён из-за истёкшего триала, хотя Вячеслав давал бессрочный доступ напрямую через s-ui. Вручную поставили `active`, expires_at=2099-12-31, enable=1
- Добавлена поддержка `/approve <user_id> 0` = бессрочный доступ (db.py + bot.py)
- Добавлена команда `/admin` — inline-меню управления пользователями: список с иконками статусов, кнопки 1/3/12 мес / Бессрочно / Отключить, пользователь получает уведомление автоматически

### Решения
- Поле `inbounds` в s-ui.db должно быть BLOB, не TEXT — использовать `.encode()` при вставке из Python
- Если давать доступ напрямую через s-ui, бот не знает об этом и планировщик отключит клиента — всегда активировать через `/admin` в боте

- Опубликован пост №3 в @santex_ai — «Панель управления за 30 минут без кода». Рубрика «Как это сделать», с картинкой-макетом /admin меню
- Запланирован пост №4 на 2026-07-02 — «Сломался VPN, починил за час». Рубрика «Делаю в прямом эфире»

### TODO на потом
- Получить TELEGRAM_API_ID/HASH для Telethon (Lead Machine)
- Кворк 3 — автоматизация (оформить и запустить)
- Бот-репетитор (7 класс)

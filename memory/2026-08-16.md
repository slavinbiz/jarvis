# 2026-08-16

## Сессия 1
Тема: проверка сайта и мини-аппа после аварии TimeWeb, фикс 404 на мини-аппе

### Сделано
- TimeWeb (64.188.57.249): SSH всё ещё таймаутится, но Вячеслав подтвердил — крипто-бот работает (значит исходящие с сервера живы, входящие 443/22 ещё нет). VPN-бот `@djarvisvpn_bot` для новых клиентов пока недоступен
- Проверил сайт `www.demin.digital` — открывается нормально, ошибок нет
- Проверил мини-апп `viacheslav-tma.vercel.app` — отдавал 404 на корне
- Диагноз: в `vercel.json` (`C:\Users\User\Documents\viacheslav-tma`) был только роут `/tma` → `/tma.html`, без роута для `/`. Старый синтаксис `routes` отключает дефолтный роутинг целиком — голый домен ловил 404 с самого первого деплоя (баг с 327245d, инициализация проекта)
- Добавил `{ "src": "/", "dest": "/tma.html" }` в vercel.json, закоммитил и запушил (`b45c1eb`)
- Проверил после деплоя — корень отдаёт мини-апп корректно, 404 исчез

### Решения
- Мини-апп теперь открывается и по голому домену, и по `/tma` — оба ведут на `tma.html`

### TODO
- [ ] Проверять статус аварии TimeWeb — SSH/443 всё ещё недоступны снаружи, крипто-бот жив по исходящим. Перепроверить когда снимут баннер, довести до конца разбор VPN-бота

## Сессия 2 — проверка после "все починили"
- Вячеслав: VPN-бот `@djarvisvpn_bot` отвечает в Telegram
- Перепроверил с моей стороны: SSH (22) и 443 на 64.188.57.249 всё ещё таймаутятся — как и раньше, бот жив по исходящим (аналогично crypto-bot 15.08), но это не доказывает восстановление входящих портов (443 нужен для реальных VPN-подключений клиентов)
- Попросил Вячеслава проверить реальное VPN-подключение клиентом (не просто ответ бота), чтобы отличить "процесс жив" от "сеть ДЦ реально восстановлена"
- Вячеслав проверил реальное VPN-подключение клиентом — не проходит. Подтверждено: сеть ДЦ Equinix FR5 всё ещё не восстановлена для входящих (443/22), несмотря на то что бот отвечает в Telegram и на сообщения "все починили". Продолжаем ждать, перепроверять по запросу
- Вячеслав прислал скриншот панели TimeWeb: баннер сменился на "Стойки операторов связи запущены, связность до площадки в Германии восстановлена" (сервер Cute Vulpecula, ID 4662767, "в сети месяц")
- Перепроверил сразу после — SSH (22) и 443 всё ещё таймаутятся с моей стороны. Расхождение между статусом ДЦ и реальной доступностью сервера — вероятно сетевой интерфейс/маршрутизация не поднялись сами после долгого простоя
- Посоветовал Вячеславу перезагрузить сервер вручную через панель TimeWeb (SSH недоступен, сделать это могу только он)
- Вячеслав перезагрузил сервер через панель TimeWeb. Проверил после ребута: ping 64.188.57.249 — 100% потерь (4/4), SSH (22) и 443 — таймаут. Хуже чем до ребута (раньше хоть пинг отвечал с TTL=128, хоть и подозрительно). Гипотеза: либо сеть до площадки ещё не полностью восстановлена вопреки баннеру, либо сервер не поднялся после ребута. Посоветовал проверить статус в панели и VNC-консоль

## Сессия 3 — SMM-субагент для @santex_s_ai
Тема: собрать и выкатить субагента `smm`, который готовит черновики постов по расписанию

### Сделано
- Собрал субагента `smm` через subagent-driven-development: план `docs/superpowers/plans/2026-08-16-smm-subagent.md`, спека `docs/superpowers/specs/2026-08-16-smm-subagent-design.md`
- Новые файлы: `.claude/agents/smm.md`; новый раздел "SMM-субагент и расписание публикаций" в `.claude/skills/santex-post-writer/SKILL.md`
- Зарегистрировал и включил два продакшн-расписания на Fornex: `smm-digest-saturday` (суббота 10:00, автопубликация без согласования) и `smm-practice-prep` (пн/ср 18:00, черновик на согласование)
- Отключил два старых пересекающихся расписания (`e8360f1a`, `111de0c2`), которые появились раньше этой работы и конфликтовали бы с новыми — дублирующиеся черновики/противоречивое поведение

- Финальное ревью всей ветки нашло 2 реальных бага (curl `-F chat_id=@...` для sendPhoto так и не работал после первого фикса — поймано живым тестом запроса; `-sf` глушил тело ошибки) и смысловые нестыковки (4 места в SKILL.md ещё утверждали "всегда согласование" даже для дайджеста) — всё исправлено одним фикс-раундом, перепроверено, чисто
- Смёржил ветку `worktree-smm-subagent` в `main` локально, запушил на GitHub, подтянул на Fornex через `git pull` — сервер реально видит `smm.md` и новый раздел скилла, до срабатывания первого расписания (пн 18:00) успел
- Поправил мелкую опечатку направления ("матрицу выше" → "ниже") в Quick Reference, засинкал
- Смоук-тест: продиспетчерил `Agent(subagent_type:"smm")` живьём — сработало (после мержа тип агента появился в реестре), черновик пришёл в правильном формате (ТИП/ТЕКСТ/КАРТИНКА/РЕКОМЕНДАЦИЯ), стиль канала соблюдён

### Открытый вопрос
- Права публикации для постов про практику (сейчас как у продажи — нужно "ок") — пересмотреть после того, как автопубликация дайджеста поработает вживую

## Итоги дня (все сессии)

### Решения
- Мини-апп `viacheslav-tma.vercel.app` больше не даёт 404 на голом домене — пофикшено и задеплоено
- Авария TimeWeb (Equinix FR5) официально помечена хостером как устранённая, но по факту сервер `64.188.57.249` (крипто-бот + VPN-бот) остаётся недоступен даже после ручного ребута — расхождение между статусом ДЦ и реальной сетью сервера, причина не установлена
- SMM-субагент `smm` для @santex_s_ai собран, задеплоен и живьём протестирован — дайджест теперь публикуется автоматически по субботам, практика готовится на согласование по пн/ср, два старых конфликтующих расписания отключены

### TODO на завтра/потом
- [ ] TimeWeb `64.188.57.249`: SSH/443/ping всё ещё не отвечают несмотря на ребут и заявленное восстановление ДЦ — проверить статус в панели/VNC-консоли (может сам Вячеслав), при необходимости писать в поддержку TimeWeb с фактами (ребут не помог, пинг 100% потерь)
- [ ] Проверить в субботу 22.08, что дайджест реально автопубликуется в @santex_s_ai по расписанию — первый живой прогон
- [ ] Практика-расписание сработает уже пн 17.08 в 18:00 — проверить, что черновик пришёл корректно и pending-хендофф на "ок" отработал

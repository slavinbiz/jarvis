# 2026-07-04

## Сессия 1

### Сделано
- Вячеслав попросил заменить триал на нормальную подписку в VPN-боте для своего аккаунта (888224075, Viacheslav_Demin)
- Проверил vpn_bot.db на TimeWeb: аккаунт уже был active до 2026-07-27 (не триал), но нужен бессрочный
- Выполнил логику `/approve 888224075 0` напрямую через одноразовый скрипт (sui.enable_client + db.activate_subscription(0) + get_config_uri + bot.send_message), т.к. это проще чем слать команду через сам Telegram
- Подписка активирована бессрочно (expires_at=2099-12-31), бот прислал Hysteria2-конфиг (UDP/8443, obfs salamander) прямо в чат

- Клиент vpnbot_888224075 в s-ui.db был привязан только к inbound [1] (hysteria2) — добавил inbound 2 (trojan-443), inbounds теперь [1,2] (бэкап s-ui.db.bak-*-trojan-vsl перед правкой)
- Собрал и отправил вручную trojan://-ссылку (TCP/443, security=tls, allowInsecure=1, тот же пароль что у hysteria2) для мобильного/Tele2

## Сессия 2 — довёл ручной фикс до системного: бот сам выдаёт рабочий профиль

Вячеслав указал: "как мы будем нерабочее приложение людям давать" — ручная сборка trojan-ссылки через SSH это костыль на одного человека, а бот должен выдавать рабочий профиль всем клиентам автоматически.

### Сделано
- Нашёл и починил баг: HY2_PORT в .env был 8443, а сервер реально слушает 443 (обнаружил через ss -ulnp/-tlnp) — все конфиги от бота с момента отката порта были нерабочие
- sui.py: create_client() теперь привязывает нового клиента сразу к обоим inbound (hysteria2 + trojan), не только к hysteria2
- sui.py: добавил build_singbox_config(user_id) — собирает единый sing-box профиль с urltest-группой (auto-переключение Hysteria2 ↔ Trojan по доступности, интервал 1 мин)
- bot.py: новая функция _deliver_config() — шлёт этот профиль файлом (jarvis-vpn.json) через Hiddify "Import file" вместо QR/текстовой ссылки на голый hysteria2-URI. Заменил во всех 4 точках выдачи: триал, оплата (cb_approve), /admin (cb_admin_action), /approve (cmd_approve)
- Смигрировал двух текущих платных клиентов (vpnbot_6124820268 produser_viacheslav, vpnbot_1120620453 Людмила) на inbounds [1,2] — раньше у них тоже не было trojan-фоллбэка на случай блокировки UDP
- Задеплоил sui.py/bot.py на сервер, добавил в .env: TROJAN_INBOUND_ID=2, TROJAN_PORT=443, OBFS_PASSWORD=salamander_vpn_2026, TLS_SNI=www.bing.com
- Проверил сквозь реальный код бота (импортировал bot.py, вызвал _deliver_config напрямую) — файл дошёл, delivered: True

### Решения
- Единый профиль (Hysteria2+Trojan+urltest) — новый стандарт выдачи для ВСЕХ клиентов бота, не только для владельца. Ручные one-off скрипты через SSH — только для диагностики, финальный фикс всегда должен идти в код бота, который задеплоен и переживёт следующую активацию/перезапуск
- Локальный C:\Users\User\projects\vpn-bot был не в git и рассинхронизирован с сервером (сервер правили напрямую через SSH несколько дней) — синхронизировал перед правкой (scp с сервера), но репозиторий так и не заведён в git, стоит сделать при следующей возможности

## Сессия 3 — правка формулировки
- Вячеслав заметил: в подписи к файлу было "открой файл ниже", а Telegram в send_document всегда рисует файл НАД подписью — поправил на "выше", задеплоил

## Сессия 4 — вернул текст вместо файла
Вячеслав: файл неудобнее текста — раньше копировал ссылку из буфера прямо в Hiddify, с файлом теперь лишние шаги (скачать, найти, Import file).

### Сделано
- _deliver_config() переписан: вместо send_document с файлом — send_message с текстом, JSON в <pre><code> для копирования одним тапом (html.escape для безопасности HTML-парсинга)
- Инструкция в подписи: «+» → «Добавить из буфера» вместо «Import file»
- Длина конфига ~1470 символов — укладывается в лимит Telegram-сообщения (4096) с запасом
- Задеплоил, проверил через реальный _deliver_config — прошло

### Заметка
- Работает то же на всех точках выдачи (триал/оплата/admin/approve), т.к. все они вызывают общий _deliver_config

## Сессия 5 — вместо простыни JSON: короткая ссылка-подписка
Вячеслав: вставлять сырой JSON текстом ("простыня") неудобно и непонятно что делать дальше. Решение — HTTP-эндпоинт на самом vpn-боте, отдающий конфиг по токену; в Hiddify вставляется короткая ссылка через "Добавить из буфера" (штатный способ добавления подписки в sing-box-клиентах).

### Сделано
- db.py: колонка sub_token у users (миграция ALTER TABLE, обёрнута в try/except), get_or_create_sub_token()/get_user_by_sub_token()
- subserver.py (новый файл): aiohttp-сервер, GET /sub/<token> -> sui.build_singbox_config(user_id) as JSON, 404 на неизвестный токен
- bot.py: main() поднимает sub-сервер параллельно с polling (aiohttp.web.AppRunner + TCPSite), корректный cleanup в finally
- _deliver_config теперь шлёт короткую ссылку http://HOST:SUB_PORT/sub/<token> вместо текста JSON
- .env: SUB_PORT=8080 (порт свободен, проверил через ss -tlnp)
- Проверено: сервер поднялся на 8080, локально curl отдаёт валидный JSON по реальному токену, _deliver_config через реальный код бота доставил ссылку

### TODO
- Вячеслав должен вручную открыть TCP/8080 в облачном файрволе TimeWeb (панель timeweb.cloud → сервер → Безопасность/Файрвол) — иначе ссылка не откроется снаружи
- После открытия порта — проверить реальный импорт в Hiddify через "Добавить из буфера"

## Сессия 6 — зависание Hiddify при подключении
Профиль по ссылке добавился, но при "Подключить" зависало всё приложение.

### Диагноз
Сам прописал в конфиге "inbounds": [{"type": "tun", ...}] — Hiddify сам управляет VPN-интерфейсом (Android VpnService), давать ему свой tun в клиентском профиле — конфликт двух VPN-интерфейсов. Профили для GUI-клиентов (Hiddify/NekoBox) должны содержать только outbounds+route, без inbounds/tun — это заводит сам клиент.

### Сделано
- sui.py build_singbox_config(): убрал секцию "inbounds" целиком, оставил dns/outbounds/route
- Задеплоил, проверил что /sub/<token> отдаёт конфиг без inbounds
- Ждём подтверждения от Вячеслава: удалить старый профиль в Hiddify, добавить заново по той же ссылке, проверить подключение

## Сессия 7 — подтверждено рабочим
После форс-обновления подписки в Hiddify (кэш старого tun-конфига) профиль подключился и на Wi-Fi, и на Tele2-мобильном через автопереключение urltest. Цифры пинга в UI (156ms Wi-Fi / 785ms мобильный) — ожидаемо: на мобильном urltest меряет через Trojan/TCP-фоллбэк (Tele2 режет Hysteria2/UDP), TCP+TLS тяжелее UDP + сервер в Германии. Не баг, Вячеслав подтвердил что реально работает нормально.

### Итог по задаче "единый рабочий VPN-профиль от бота"
- Бот выдаёт короткую ссылку-подписку (http://HOST:8080/sub/<token>) вместо голого URI или сырого JSON
- Профиль без inbounds/tun (эту часть настраивает сам Hiddify) — Hysteria2+Trojan+urltest автопереключение
- Все текущие клиенты (личный + 2 платных) переведены на оба протокола, новые триалы получают оба сразу через create_client()
- Порт 8080 (TCP) открыт в облачном файрволе TimeWeb
- Статус: ЗАКРЫТО, работает на Wi-Fi и Tele2-мобильном

## Сессия 8 — разослал обновлённый профиль двум платным клиентам
- Людмила (1120620453, active до 2027-06-26) и produser_viacheslav (6124820268, бессрочно) — оба уже были мигрированы на inbounds [1,2], но не получали новый профиль через бота
- Разослал вручную (sui.enable_client + _deliver_config, без изменения expires_at/status) — не трогал их реальные подписки, только обновил доставку конфига
- Оба сообщения доставлены (delivered: True)

# 2026-09-11

## Сессия 1

### Сделано
- Почта slavin68@mail.ru: 10 непрочитанных, всё инфомусор (Андрианов, Червонец, iiuniversitet, ПрактикиPRO, Спектор) — по просьбе Вячеслава перенесены в Корзину (COPY+expunge через IMAP), инбокс чист
- Разобрал сигналы crypto-bot за сегодня (до 08:23 МСК): 4 детекта памп/дампа (SCUSDT×2, SOLVUSDT×2), ни один не дошёл до входа — R:R-гейт сегодня вообще не срабатывал, потому что ни у одного детекта не сложился разворотный паттерн + дивергенция RSI. Один явно отменён по логу (пробой пика), три остальных истекли молча — в коде (`crypto_bot.py`, ветка `candles_left <= 0`) нет log.info на обычный таймаут наблюдения, только на отмену по пробою пика и на вход/отказ по R:R

- Добавил `log.info` в ветку молчаливого таймаута наблюдения (`candles_left <= 0`) в crypto_bot.py — задеплоено и перезапущено
- Разобрал по свечам с Binance сигнал SOLVUSDT 07:25 МСК, который Вячеслав назвал «почти идеальным»: медвежье поглощение реально сложилось, но дивергенция RSI не прошла (цена к моменту паттерна уже откатила ~5% от пика, сравнивать не с чем — определение дивергенции заточено под повторный тест хая, а не под паттерн после отката)
- **RSI-дивергенция перестала быть гейтом входа** (`crypto_bot.py`) — теперь вход публикуется по паттерну (пин-бар/поглощение) + R:R≥1:3, дивергенция считается и показывается в сообщении строкой «RSI-дивергенция: есть/нет» как пометка силы сигнала, не условие. Задеплоено на TimeWeb (scp + restart), сервис active

### Решения
- **Дивергенция RSI — желательный, необязательный признак**, не блокирует вход. Решение Вячеслава 11.09.2026 после разбора сигнала SOLVUSDT, который дивергенция отсеяла зря

### TODO на потом
- Понаблюдать, как часто теперь проходят входы без дивергенции и какого они качества — если начнёт мусорить, вернуться к вопросу

## Сессия 2 — PlayfulPages/«Трафик Барановой»: чиним генератор лабиринтов

### Сделано
- Вернулись к проекту PlayfulPages/«Трафик Барановой» — статус на сегодня: ЮKassa Вячеслав ещё не регистрировал, не готов пока (ИП/самозанятость, счёт, паспорт — берёт время)
- Вячеслав вчера вечером с DeepSeek написал свой HTML-генератор лабиринтов (`C:\Users\User\Documents\ИИ и прочее\трафик барановой\2 недея\maze.html`) вместо использования штатного инструмента курса (Stock Script) — попробовал, результат показался «косоватым»
- Разобрал код: подход правильный (процедурная генерация через граф+DFS/BFS на канвасе, не рисование картинки нейросетью — для лабиринтов иначе и нельзя, генеративка не понимает топологию «один решаемый путь»)
- Нашёл реальный баг через рендер в headless-браузере (playwright, скриншот до/после): зелёная стрелка входа рисовалась по одной формуле (середина верхнего ряда), а маршрут решения (красная линия) считался от жёстко зашитого угла (0,0) — дверь на картинке и путь решения не совпадали. На печати выглядело бы как лабиринт с дверью в никуда
- Поправил `redraw()` в maze.html — старт BFS-решения теперь берётся из той же клетки, что рисуется как вход. Проверил скриншотом: красная линия решения теперь стартует ровно от зелёной стрелки. Правка внесена прямо в файл в папке курса (не в общем репозитории — это личный материал курса, не наш проект)

### Решения
- Для «Трафик Барановой»/PlayfulPages ждём, пока Вячеслав сам оформит ЮKassa — раньше делать нечего
- Генератор лабиринтов Вячеслава — рабочий подход (код, не ИИ-картинка), просто требовал правки логики, а не переделки с нуля

### TODO на вечер
- При «Входов» > 1 в maze.html дополнительные двери декоративные — решение (красная линия) рисуется только для первой. Если печатать с несколькими входами, чинить отдельно
- Топологии «Соты» и «Треуг.» в maze.html вообще без стрелок входа/выхода — только «Квадраты» и «Круг» промаркированы. Спросить у Вячеслава, нужны ли эти формы вообще, или хватит квадратов
- Продолжить разбор курса «Трафик Барановой» — судоку/криптограммы и другие форматы (уроки 16-25) ещё не пробовали

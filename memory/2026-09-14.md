# 2026-09-14

## Сессия 1

### Сделано
- Почта slavin68@mail.ru: 43 непрочитанных, инфомусор (Андрианов, Червонец, Спектор, iiuniversitet, ПрактикиPRO) — ничего важного
- Срочный запрос: репетитор по английскому, 7 класс, для ребёнка. Разобрали формат — свой Jarvis-бот и отдельный бот-репетитор (идея в PROJECTS.md) заняли бы слишком много времени

### Решения
- **Репетитора по английскому 7 класс делаем через DeepSeek напрямую, не через Claude Code** — Вячеслав сам вставляет туда мой промпт и занимается с ребёнком в чате DeepSeek. Причина: бесплатно, без расхода токенов Claude Code (та же логика экономии, что в MEMORY.md → «Экономия токенов через DeepSeek»). Подготовил системный промпт (роль репетитора, объяснения на русском, примеры на английском, уровень 7 класса, не даёт готовый ответ сразу — сначала объясняет и просит попробовать самому). Уточнил Вячеславу: DeepSeek помнит диалог только внутри одного открытого чата, новый чат = с нуля
- Идея отдельного бота-репетитора (aiogram + DeepSeek + Claude Haiku/GPT-4o mini, из PROJECTS.md) не отменена, просто не срочная — делать когда будет время

### TODO на потом
- Если формат «промпт в DeepSeek» приживётся и понадобится не только английский — вернуться к идее бота-репетитора из PROJECTS.md

## Сессия 2 — «Трафик Барановой»: криптограмма (урок 17), пробуем DeepSeek

### Сделано
- Сверился: судоку (13.09) готово и рабочее, Вячеслав подтвердил вживую — двигаемся к следующему формату (криптограмма, урок 17)
- Согласно решению 13.09 («для больших генераторов сначала пробовать DeepSeek») — написал промпт `трафик барановой/2 недея/deepseek-task-cryptogram.md`. Внутри: точный CSS из sudoku.html скопирован дословно (чтобы не плыл стиль между генераторами продукта), структура сайдбара, механика классической криптограммы (шифр-биекция A-Z→1-26, банк из 3 категорий английских фраз + своя фраза, подсказки 0/2/4 буквы, показать решение, word-wrap по canvas), явно попросил при обрыве ответа делить на 2 части, не рвать `<script>` на середине функции (урок 12.09 про обрыв длинных ответов)

### TODO
- Ждём, что Вячеслав прогонит промпт через DeepSeek и принесёт результат — дальше интегрирую в `cryptogram.html`, проверю playwright-скриншотами (как maze/sudoku)

### Готово — DeepSeek вернул файл целиком, не оборвался
Вячеслав принёс полный `cryptogram.html` от DeepSeek с первой попытки (промпт с копией CSS + явным требованием не рвать `<script>` сработал — обрыв 12.09 на файле ~900 строк не повторился на этом ~430-строчном файле). Разобрал код перед сохранением, нашёл 2 мелких бага, поправил сразу:
- Опечатка в банке фраз: `EIFEL TOWER` → `EIFFEL TOWER`
- `.status` выводил `\n` в тексте, но CSS не имел `white-space: pre-line` — перенос строки не отображался, фраза и счётчик подсказок слипались в одну строку

Сохранил в `трафик барановой/2 недея/cryptogram.html`. Проверил playwright-скриншотами (headless Chromium, как обычно): категории Animals/Space/Fun Facts, подсказки 0/2/4, показать решение (подсказки чёрным, довписанное красным — та же логика, что в судоку), своя фраза с запятой/восклицательным/точкой, увеличение шрифта до переноса на 2 строки, отдельный зум-тест на "WORLD!" — все 5 букв и прочерков на месте, знак препинания не съедает соседний прочерк. Ошибок в консоли нет.

### Итог по DeepSeek-делегированию (второй прогон)
В отличие от 12.09 (файл ~900 строк, обрыв на 752-й) — на файле ~430 строк с явной инструкцией "не рви script" DeepSeek отдал всё за один раз. Вывод: для генераторов такого масштаба (не more ~500 строк) прямой промпт с готовым CSS работает с первого раза, экономит и токены, и раунды правок.

### TODO на потом
- Следующий формат — «Скопируй картинку» (урок 18)
- Файл `cryptogram.html` не в git (личный материал курса, как maze.html/sudoku.html)

# 2026-08-14

## Сессия 1
Тема: попытка вернуться со платного Happ на старый бесплатный Hysteria2-туннель (TimeWeb) — не подтверждено, нужна помощь Вячеслава

### Сделано
- Проверил старую связку: `C:\v2rayN\hy2-client\config.json` (sing-box, Hysteria2, TimeWeb 64.188.57.249:443, obfs salamander) и ярлык `Hysteria2-VPN-Tunnel` в автозагрузке — всё на месте, не удалено
- Сервер (TimeWeb, s-ui): сервис активен, слушает UDP/443 (hysteria2) и TCP/443 (trojan), клиент `test-july` включён (enable=1), не истёк (expiry до конца 2026) — настройки сервера совпадают с локальным конфигом
- Перезапустил sing-box с логом — локальный порт 10810 поднимается, но реальное соединение до сервера обрывается по таймауту: `outbound/hysteria2: timeout: no recent network activity`. tcpdump на сервере во время попытки подключения — 0 пакетов дошло вообще
- Собрал тестовый конфиг на Trojan (TCP/443, тот же сервер, тот же self-signed сертификат) — тоже не прошёл: `read tcp ...: An existing connection was forcibly closed by the remote host`
- **Важная находка:** на этой машине сейчас Happ держит системный default route (`route print` → `0.0.0.0/0` через `172.18.0.1` с метрикой 0, обгоняет обычный Wi-Fi-маршрут с метрикой 50) — то есть ВЕСЬ трафik машины, включая мои тестовые sing-box-соединения, физически шёл через Happ, а не напрямую через провайдера. Мои тесты не чистые — не могу сказать, блокирует ли это провайдер или как-то фильтрует сам Happ
- Не смог добавить host-route в обход Happ TUN только для 64.188.57.249 — `route add` требует прав администратора, у агента их нет на этой машине (тот же паттерн, что и с WireGuard 08.08)
- Не стал отключать Happ полностью для чистого теста — моя же сессия Claude Code сейчас идёт через `HTTPS_PROXY=127.0.0.1:10809` (движок Happ), риск потерять связь с Anthropic посреди диагностики

### Решения
- НЕ переключил `start-claude.ps1` обратно на 10810 — Hysteria2-туннель сейчас физически не пробивается (ни разу пакет до сервера не дошёл), переключение сломало бы Claude Code
- Нужен чистый тест без Happ в маршруте — либо Вячеслав на 20-30 сек отключает Happ (через трей/GUI) пока я гоняю curl/sing-box, либо запускает `route add 64.188.57.249 mask 255.255.255.255 192.168.1.1 metric 1` из PowerShell от администратора

### TODO
- [x] Вячеслав выполнил от администратора `route add 64.188.57.249 mask 255.255.255.255 192.168.1.1 metric 1` — точечный маршрут в обход Happ добавлен
- [x] Перепроверил Hysteria2 через прямой маршрут — снова таймаут, пакеты до сервера не доходят

### Диагноз (сессия 1, финал)
- С прямым маршрутом (в обход Happ) НЕ проходит уже даже TCP/22 (SSH) и ping — `Test-NetConnection` и `ssh` оба таймаутятся напрямую через провайдера, хотя через Happ SSH к этому же серверу работает без проблем
- Вывод: это не блокировка конкретно Hysteria2/UDP или Trojan/TCP-443 — провайдер (TSPU) блокирует **сам IP 64.188.57.249 целиком** для прямых соединений. Никакой протокол на этом сервере это не обойдёт, пока IP в блок-листе
- Happ работает, потому что его трафик идёт через собственную инфраструктуру Happ и подключается к TimeWeb уже не с домашнего IP Вячеслава — блокировка провайдера это соединение не видит

### Решения
- Возврат к бесплатному Hysteria2/Trojan на TimeWeb (64.188.57.249) невозможен без смены сервера — IP заблокирован провайдером целиком, не только протокол
- Два пути дальше: (а) поднять Hysteria2/Trojan на новом сервере с чистым IP (например Fornex 213.193.196.125, уже оплачен под Джарвиса), (б) вернуться к WireGuard на Fornex — там проблема была не в блокировке сети, а в баге драйвера Wintun на Windows (адаптер не создавался), см. сессии 08.08. Предложил Вячеславу выбрать — начали с варианта (б), ближе к результату

### TODO
- [x] Убрать точечный маршрут `route delete 64.188.57.249`

## Сессия 2
Тема: WireGuard на Fornex — снова тот же баг Wintun, переключились на Hysteria2

### Сделано
- Вячеслав запустил WireGuard заново — то же самое: ошибка создания адаптера (тот же конфликт Wintun, что и 08.08, не решён даже после чистого перезапуска). Дополнительно вылезло зависание самого приложения: 4 процесса `wireguard.exe` в памяти без единого окна — три из них не смог снять сам (Access denied), попросил Вячеслава закрыть через трей/диспетчер задач
- Решили не копать Wintun дальше (тупиковый вариант, тот же результат что и в прошлый раз) — переключились на Hysteria2 на Fornex вместо WireGuard

### Установка Hysteria2 на Fornex (213.193.196.125)
- Официальный инсталлятор `bash <(curl -fsSL https://get.hy2.sh/)` — standalone-сервер, НЕ s-ui (s-ui избыточен для одного личного клиента, сервер и так на 1GB RAM)
- Self-signed TLS-сертификат: `openssl req -x509 -nodes -newkey ec -pkeyopt ec_paramgen_curve:prime256v1 -keyout /etc/hysteria/server.key -out /etc/hysteria/server.crt -subj '/CN=www.bing.com' -days 3650`
- `/etc/hysteria/config.yaml`: `listen: :443`, auth password (сгенерирован `openssl rand -base64 16`), obfs salamander (пароль `fornex_salamander_2026`), masquerade proxy на bing.com
- systemd `hysteria-server` enabled+started, слушает UDP/443, файрвол на Fornex не мешает (ufw inactive, iptables ACCEPT ALL, в отличие от TimeWeb там нет отдельного облачного файрвола)
- Windows-клиент: обновил `C:\v2rayN\hy2-client\config.json` на новый сервер/пароли, порт 10810 не менял — ярлык `Hysteria2-VPN-Tunnel` в автозагрузке трогать не пришлось, он ссылается на тот же файл конфига
- **Проверено дважды:** через Happ (как временный транспорт) и напрямую (Вячеслав дал точечный маршрут в обход Happ, аналогично тесту с TimeWeb) — прямое подключение проходит, внешний IP через туннель = 213.193.196.125, console.anthropic.com и claude.ai отдают нормальные коды (301/302) с браузерным UA
- Убрал точечный маршрут после проверки: `route delete 213.193.196.125`

### Обновил
- `scripts/start-claude.ps1` — обратно на 10810 (свой туннель) вместо Happ/10809, с BOM
- `knowledge/claude-code-windows.md` — заменил инструкции Happ на Hysteria2/Fornex, Happ переведён в раздел «запасной VPN»
- `MEMORY.md` — Fornex теперь основной личный VPN, TimeWeb помечен как заблокированный провайдером для прямых соединений, WireGuard на сервере оставлен про запас но не используется с Windows

### Решения
- Личный VPN переехал с TimeWeb (64.188.57.249, IP заблокирован провайдером) на Fornex (213.193.196.125), протокол Hysteria2 стандалон (не s-ui) — снова бесплатный (в рамках уже оплаченного сервера), без Happ
- WireGuard на Windows для этой машины — тупик, баг Wintun не чинится штатными средствами, дальше не пробовать без переустановки/чистки драйверов вручную
- Happ остаётся установленным как платный запасной вариант на случай если новый IP тоже заблокируют

### TODO
- [x] Удалить WireGuard с Windows-машины — снесён через `msiexec /x {99A54A94-4BE0-4374-B3A6-F504E826DDF8} /qn` от администратора, реестр и `C:\Program Files\WireGuard` чистые (см. сессия 3). Убрал заодно `client.conf` и лог с рабочего стола
- [x] Ярлык Chrome с `--proxy-server=socks5://127.0.0.1:10810` — оказался уже настроен с 25 июня (`Chrome VPN.lnk` на рабочем столе, отдельный `--user-data-dir`, Яндекс не трогает), просто не был отмечен как готовый. Порт актуален (10810). Вячеслав проверил вживую — показывает Германию, работает
- [ ] Проверить через несколько дней, не попал ли IP Fornex тоже в блок-лист провайдера (паттерн уже видели дважды на других серверах)

### Итог дня
Личный VPN полностью переехал с платного Happ на собственный Hysteria2-туннель (Fornex, порт 10810) — покрывает и Claude Code (`start-claude.ps1`), и браузер (ярлык «Chrome VPN»). WireGuard и его баг с Wintun закрыты как тупиковая ветка, WireGuard снесён с машины. Happ остался установленным как платный запасной вариант на крайний случай.

## Сессия 3
Тема: деинсталляция WireGuard с Windows

### Сделано
- Первая попытка `msiexec /x {GUID} /qn` не сработала — 0 записей в MsiInstaller event log, скорее всего PowerShell был открыт не от администратора
- Вторая попытка (с `/l*v` логом) тоже не оставила следов — судя по всему, тот же PowerShell не был elevated, хотя выглядел как admin-сессия
- Проверили явно: `([Security.Principal.WindowsPrincipal]...).IsInRole(...Administrator)` → `True` — только после этого прогнали `Start-Process msiexec.exe -ArgumentList ... -Wait`, сработало с первого раза, лог подтвердил `Removal completed successfully`
- Урок: перед retry долгих админ-команд стоит сразу проверять elevation одной строкой, не гонять одну и ту же команду вслепую несколько раз
- Остался "призрак" Wintun-адаптера в диспетчере устройств (Disconnected, без IP) — сама деинсталляция не убрала виртуальный адаптер, скорее всего уйдёт после перезагрузки. Не критично, не мешает Hysteria2-туннелю (тот работает в юзерспейсе, Wintun не трогает)

### Решения
- WireGuard для этой машины закрыт как направление — снесён полностью, Hysteria2/Fornex остаётся единственным личным VPN

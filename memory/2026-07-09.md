# 2026-07-09

## Сессия — ссылка на сайт в память, проверка Lead Machine, ретраи в relevance-фильтре

### Сделано
- Нашёл и зафиксировал в MEMORY.md ссылку на сайт (демин.диджитал → https://www.demin.digital/, проксирует Vercel-деплой viacheslav-digital.vercel.app). В описании канала @santex_s_ai ссылки на сайт нет — добавление туда не потребовалось, Вячеслав хотел просто саму ссылку
- Проверил сайт через WebFetch — открывается, все секции на месте. Вячеслав подтвердил вручную — работает
- Проверил Lead Machine на сервере: `app` контейнер работал 21 час без рестартов, база выросла с 7 до 10 лидов с прошлой проверки (06.07). Новые: id 8 (06.07), id 9 mari_vakansii (08.07, релевантный, но пустой contact_username), id 10 profiwork (09.07, ложное срабатывание — человек сам предлагает услуги, а не ищет специалиста)
- Нашёл причину проскока id 10 мимо AI-фильтра: `is_relevant_lead()` падал на первой же ошибке OpenRouter (Connection error после исчерпания встроенных ретраев openai-клиента) и работал fail-open — сохранял лид без проверки
- Пофиксил: `app/finder/relevance.py` — 3 попытки с паузой 5с перед fail-open. Тесты: `test_is_relevant_lead_retries_then_succeeds` (новый) + патч `asyncio.sleep` в тесте на постоянный отказ, чтобы не спать реально. Коммит `d2ed7d3` в `slavinbiz/lead-machine`, запушено
- Задеплоил на сервер тарболлом (app/ + tests/), собрал образ, прогнал полный набор тестов **внутри контейнера** — 45 passed (локально без fastapi доступно только 41, test_search_loop.py пропускается). Перезапустил app

### Решения
- Лиды без `contact_username` — не трогаем, они и так не участвуют в рассылке (`main.py:89` их пропускает), просто безвредный шум в таблице
- Fail-open в relevance-фильтре оставили как принцип (лучше немного шума, чем потеря лида), но добавили ретраи, чтобы обычные сетевые сбои не давали фильтру сдаваться с первого раза

### TODO
- [ ] Последить ещё несколько дней — станет ли меньше ложных срабатываний вроде id 10 после ретраев
- [ ] Подумать над отдельной логикой для лидов без contact_username (сейчас просто зависают в базе) — не критично, но со временем таблица засоряется
- [ ] Из прошлой сессии остаётся открытым: отправить 3 сообщения outreach (знакомые/чат курса/коллеги), найти и вступить в 2-3 чата по вайбкодингу/фрилансу

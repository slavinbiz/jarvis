# 2026-09-08

## Сессия 1

### Сделано
- Проверил почту slavin68@mail.ru (72 непрочитанных) — почти всё инфомусор (Андрианов/Червонец/PraktikPro/iiuniversitet), по просьбе Вячеслава отправил 65 писем в корзину, оставил 7 чеков/уведомлений (Fornex, е-ОФД, CloudKassir, WhiteVPN)
- Уточнил: WhiteVPN (300₽/мес, автопродление) — личный платный VPN Вячеслава, не подозрительная активность. Записал в MEMORY.md
- Собрал дашборд статистики сигналов крипто-бота Pamp-Damp: выгрузил `/root/signals.db` с TimeWeb (1705 сигналов, 19.07–08.09.2026), посчитал винрейт по горизонтам проверки (15/60/240 мин), по метке тренда, по направлению (long/short), по неделям и по топ-10 монетам. Опубликовал как артефакт (dataviz-скилл, тема light/dark, интерактивные тултипы) — https://claude.ai/code/artifact/13a59824-8f1b-4119-a314-e72efc50cfc0

### Решения
- **Ключевой вывод по крипто-боту:** винрейт сырых сигналов на всех горизонтах 37–38% — ниже монетки. Сигнал сам по себе не точка входа, нужен доп. фильтр (EMA-тренд слабо помогает: «против тренда» и «по тренду далеко от EMA20» дают одинаковый винрейт ~37%)
- Найден баг записи в crypto-bot: `pair_name` для 25 монет пишется непоследовательно — то `SYMBOL/USDT`, то `SYMBOLUSDT` (видимо, менялся формат в коде на середине периода сбора). На статистику не повлияло (считал по полю `symbol`), но стоит починить в самом боте, если Вячеслав будет там что-то менять

- crypto-bot: два фикса по просьбе Вячеслава — унифицировал формат `pair_name` (всегда `SYMBOL/USDT`, добавил `format_pair_name()`, задним числом поправил все 1705+128 строк в живой `signals.db`); перевёл детект памп/дамп с 1-минутного графика на 5-минутный (`INTERVAL = "5m"`). Все зависящие от таймфрейма окна (LOOKBACK 2 часа, PUMP_WINDOW 60 мин, TREND_PERIOD 60 мин, стейл-тайм проверки) пересчитаны через новую `INTERVAL_MINUTES`, чтобы не потерять смысл в реальном времени при смене свечи. Заодно нашёл и починил баг подписи: текст сигнала писал "за N мин", где N — число свечей, а не минуты (совпадало на 1м, разъехалось бы на 5м). Оба фикса закоммичены, запушены в `slavinbiz/crypto-bot`, задеплоены на TimeWeb, сервис перезапущен и переподключился без ошибок (110 пар, WebSocket на новом стриме `kline_5m`)

- crypto-bot: по просьбе Вячеслава убрал целиком контр-сигнал по недельным EMA («🎯 Контр-сигнал», модуль `ema_pullback`) — единственное, что помимо памп/дампа реально слало сообщения в канал. Удалены `send_pullback_signal`/`check_pullback_tracking` и их вызовы, весь CRUD по таблице `pullback_tracking`, создание этой таблицы, `import ema_pullback` — 222 строки. Старые данные в таблице на сервере не тронуты, просто не растут. Теперь бот шлёт только памп/дамп с 5м графика. Закоммитил, запушил, задеплоил, перезапустил — переподключился чисто (110 пар)

- По просьбе Вячеслава очистил `signals.db` на TimeWeb от старых данных (1705 сигналов 1м-эры + 4960 проверок + 128 pullback_tracking) — начинаем копить статистику заново на 5м-таймфрейме. Команда `DELETE FROM` в deny-листе `.claude/settings.json` — сознательный барьер, чат-подтверждение его не обходит. Дал готовую команду, Вячеслав выполнил сам через `!` в своём чате, я проверил результат (`0|0|0`, файл 424КБ→20КБ после VACUUM)

- Сузил окно детекта памп/дампа с 60 до 20 минут (12→4 свечи по 5м) — по просьбе Вячеслава сигнал должен быть явным ускорением на коротком отрезке, а не любым набросом 6%+ за растянутый час. Задеплоил, перезапустил — чисто, 111 пар

- **Новая разворотная стратегия входа** — по скриншоту графика от Вячеслава (PLAYSOUTUSDT, пример памп→разворот). После памп/дамп-сигнала бот больше не входит сразу, а ставит пару под наблюдение на 6 свечей (30 мин) и ждёт пин-бар (хвост отказа ≥2x тела и ≥60% диапазона свечи) в СТОРОНУ, ОБРАТНУЮ рывку — памп вверх → шорт по медвежьему пин-бару, дамп вниз → лонг по бычьему. Подтвердил у Вячеслава направление (против движения, как на его примере) перед реализацией. При подтверждении шлёт отдельное сообщение «🎯 Вход» и логирует как обычный сигнал (дальше работает verify_signal 15/60/240 мин). Старое памп/дамп-сообщение осталось как информационное. Проверил логику is_pin_bar на трёх примерах свечей вручную — работает верно. Задеплоил, перезапустил — чисто, 111 пар

### TODO на потом
- Дашборд статистики сигналов (артефакт от сегодня) теперь показывает 0 — это ожидаемо, копить новую статистику на 5м/4-свечи-эре заново
- Понаблюдать за первыми реальными пин-бар-входами в канале — параметры (6 свечей ожидания, хвост ≥2x тела/≥60% диапазона) взяты по стандартному определению пин-бара, не откалиброваны на исторических данных этого бота
- Обсудить с Вячеславом, стоит ли добавлять доп. фильтр к сигналам (винрейт на 1м был 37-38%, посмотреть какой станет на 5м после накопления новых данных — сравнивать больше не с чем, старые данные стёрты)

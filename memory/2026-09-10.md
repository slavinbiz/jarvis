# 2026-09-10

## Сессия 1

### Сделано
- Почта slavin68@mail.ru: 11 непрочитанных, всё инфомусор (Андрианов, Червонец, iiuniversitet, aibasis, Envybox) — ничего важного
- Разобрал по скринам сигнал ACEUSDT LONG (дамп −6.98% → бычий пин-бар → дивергенция RSI подтверждена) — объяснил вход/стоп/тейк
- Посчитал R:R по этому сигналу: риск 0.0177, прибыль 0.0092 → R:R ≈ 1:0.52, невыгодно
- crypto-bot: добавил гейт `MIN_RISK_REWARD = 3.0` — вход публикуется в канал только при R:R ≥ 1:3 (тейк/стоп от входа), иначе наблюдение снимается молча. Без валидного тейка (уровень уже пройден) — тоже провал фильтра. Задеплоено на TimeWeb (scp + restart), сервис active

### Решения
- **R:R ≥ 1:3 — обязательный фильтр перед публикацией входа**, встроен в код бота, а не оставлен на ручной отбор (подтвердил Вячеслав)

- Разобрал жалобу на сигнал HEMIUSDT (01:05) — по логам и БД (`/root/bot.log`, `/root/bot.log.1`) не нашёл ни дамп-триггера, ни строки «Вход» для этой записи, а entry_price в БД (id=33) подозрительно круглый (0.007) — похоже на старую тестовую запись, а не реальный сигнал от бота этой ночью. Спросил у Вячеслава, точно ли было сообщение в Telegram — ответа пока нет
- Объяснил механику: гигантская свеча-выброс (флэш-вик/стоп-хант) с большой вероятностью и так режется гейтом R:R — либо тейк уже пройден внутри самой свечи (take_price=None → провал), либо стоп по такой свече слишком широкий для R:R≥3. Явный отдельный фильтр на «аномальный диапазон свечи» пока не добавлял — Вячеслав решил сначала последить, что реально проходит через текущий фильтр

### Решения
- **Пока не добавлять доп. фильтр на аномально широкие свечи-выбросы** — сначала смотрим, как R:R-гейт отсеивает сигналы на практике

### TODO на потом
- Понаблюдать, сколько сигналов проходит через фильтр R:R≥3 и какие — если пропускает флэш-свечи или слишком мало сигналов вообще проходит, обсудить с Вячеславом доп. фильтр или калибровку порога

### Уточнение
- Вячеслав проверил канал — сигнала входа на 01:05 по HEMIUSDT в реальности не было, все сообщения ушли в 01:10 (это детект памп/дамп, не вход на разворот). Подтвердило: запись id=33 в signals.db — протухшая тестовая, не реальный сигнал. Ложную тревогу закрыли
- Протухшую запись id=33 удалил сам Вячеслав (DELETE в deny-листе) — проверил, обе таблицы чистые

### Итог дня
crypto-bot получил гейт минимального R:R (1:3) — ключевое изменение дня, по просьбе Вячеслава. Разобрали механику разворотного входа на примерах (ACEUSDT, HEMIUSDT), заодно почистили одну протухшую тестовую запись в БД. Дальше просто наблюдаем, как фильтр отсеивает сигналы на живом потоке.

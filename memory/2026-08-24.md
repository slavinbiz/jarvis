# 2026-08-24

## Сессия 1

### Сделано
- Разобрал транскрипт воркшопа Камала Сатавова «Создание нейромультиков» (PDF, прислал Вячеслав) — сохранил пайплайн в `knowledge/neuromultiki-pipeline.md`
- Просмотрел папку Telegram Desktop Downloads на новые материалы курса — нашёл пакет из 5 файлов, упавших одним пакетом сегодня в 11:55-11:56 (транскрипт+конспект нейромультиков + 3 методички)
- Разобрал 3 методички: конспект нейромультиков (добавил чистовые промпты в knowledge-файл), AutoMontage-Agent (сохранил новым файлом `knowledge/automontage-agent.md` — доверенный источник Ntmib), Metricool MCP (дополнил существующую запись в MEMORY.md конкретными шагами установки)
- Методичку «50 инструментов для агента» (gstack от Гарри Тана) не сохранял как рекомендацию — маркетинговый текст, репозиторий не проверен, `git clone`+`./setup` с чужого гитхаба вслепую не запускать

- Установил AutoMontage-Agent по просьбе Вячеслава: `C:\Users\User\projects\AutoMontage-Agent\` (git clone), ffmpeg через winget (Gyan.FFmpeg 9.0), npm install (315 пакетов), pip install (faster-whisper 1.2.1), `npm install -g .` → команда `automontage` глобально. `automontage doctor` — всё зелёное, кроме опционального Chromium

- Прогнал `automontage demo` — первый реальный тест инструмента. 14-секундный демо-ролик (1080x1920), рендер ~2 мин, exit code 0, файл `out/demo.mp4` (1.5MB). Монтажный лист валиден, гейт качества чист, но 3 предупреждения по динамике (нормально для дефолтного тестового листа, не баг): текст на экране 87% времени, 3 одинаковые позиции подряд, все 4 плашки не синхронны с речью

### Решения
- Пайплайн нейромультиков — держим как справочную информацию, не запускаем без отдельного решения Вячеслава
- AutoMontage-Agent установлен, `demo` прогнан успешно — инструмент рабочий. Следующий шаг — реальное видео, не демо-лист

- Аналитика крипто-бота по просьбе Вячеслава (`signals.db` на TimeWeb, 1355 сигналов с 19.07): общий винрейт 37% в плюс / 56% в минус на 60-мин проверке, ожидание отрицательное (~-0.6%/сделку без фильтра входа). Разобрал EMA-тренд фильтр (`ema_trend.py`) — винрейт одинаковый (~39-40%) что «по тренду», что «против тренда», на всех трёх горизонтах проверки (15/60/240 мин). Причина: 1H EMA50/EMA200 — макро-тренд за ~41 день, не связан с исходом вспышки волатильности на минутах
- **Убрал EMA-тренд фильтр из крипто-бота полностью** по решению Вячеслава. Правки в `crypto_bot.py`: удалён `fetch_trend_verdict`, вызов в обработчике сигналов, `EMA_DISTANCE_THRESHOLD_PCT` из настроек. `ema_trend.py` (сам файл, функция `calc_ema`) оставлен — нужен `ema_pullback.py` для недельного контр-сигнала, это отдельная фича, не трогал. Тесты: удалил `test_fetch_trend_verdict.py` и `test_settings.py` (тестировали убранное), 67/67 остальных прошли. Задеплоено на TimeWeb (`scp` в `/root/`, `systemctl restart crypto-bot`), сервис `active`, лог чистый (484 пары загружены, поллинг идёт). Закоммичено и запушено в `slavinbiz/crypto-bot`

### Решения
- EMA-тренд фильтр в крипто-боте признан бесполезным (нет предиктивной силы на данных за месяц) и убран — решение Вячеслава 24.08.2026 после разбора аналитики
- Контр-сигнал по недельным EMA (`ema_pullback.py`) не трогали — отдельная фича, вопрос не поднимался

### TODO на потом
- Попробовать `automontage` на реальном своём видео (не демо-листе)
- Помнить: ffmpeg не виден в PowerShell-сессиях, открытых до 24.08.2026 — новый терминал решает
- Если захочется заменить EMA-фильтр на что-то другое (направление последних свечей 5м/15м перед сигналом, объём/дисбаланс стакана) — не предлагал сам, ждать запроса Вячеслава

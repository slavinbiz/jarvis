# 2026-09-06

## Сессия 1

### Сделано
- Проверил почту slavin68@mail.ru — из важного: Fornex прислал напоминание об истечении услуги 34-343230 (сервер 213.193.196.125) завтра, 7 сентября, автопродление спишет с баланса если средств хватит. Остальное — рассылки (aibasis, Андрианов, Червонец)
- Настроил logrotate для `/root/bot.log` крипто-бота на TimeWeb (`/etc/logrotate.d/crypto-bot`: daily, 7 ротаций, compress, delaycompress, copytruncate) — сделал сразу с Windows-машины (root, TODO с 05.09). Форсировал первую ротацию вручную: `bot.log` был 120М, теперь 0 (пишется заново), старый лог в `bot.log.1` (120М, сожмётся при следующей ротации по правилу delaycompress). `crypto-bot` не переставал работать (copytruncate не рвёт файловый хендл процесса), сервис active. Проверил что `logrotate` есть в `/etc/cron.daily/` — будущие ротации пойдут сами
- Мини-апп (viacheslav-tma): по фото от Вячеслава нашёл баг — на вкладке «О себе» подпись и биография захардкожены `#ccc`/`#ddd` под тёмную тему Telegram, у Вячеслава светлая тема → текст почти невидим (белый на белом). Заменил на `var(--hint)`/`var(--text)`, закоммитил и запушил в `slavinbiz/viacheslav-tma` (master, автодеплой Vercel)
- Проверил остальные вкладки на тот же класс бага — текстовых захардкоженных цветов больше не нашёл, всё остальное уже на переменных темы. Нашёл смежное: разделители/бордеры (таб-бар, `.divider`, `.btn-secondary`, вертикальные разделители статистики) захардкожены `rgba(255,255,255,X)` под тёмный фон — в светлой теме белая линия на белом, невидимо. Завёл переменные `--border-soft`/`--border-strong` (`color-mix(in srgb, var(--text) 8%/14%, transparent)`), заменил все 5 мест — подстраиваются под обе темы автоматически
- Проверил `C:\Users\User\Documents\ИИ и прочее\вайбкодинг\Ледовских ДУБЛЬ` по просьбе Вячеслава — новый материал курса от 05.09.2026: 4 урока про скиллы/плагины (TrendRadar для поиска готовых скиллов/идей на GitHub, что такое скилл/плагин/pipeline, стартовый набор Discovery Interview+Full-Stack Engineer+Superpowers, скилл-мастер для создания своих скиллов). Стартовый набор и скилл-мастер (`superpowers:writing-skills`) в Jarvis уже стоят — ничего ставить не пришлось, курс просто подтверждает текущую настройку
- Слетела OAuth-сессия Claude Code на Fornex под `agent` (`claude auth status` → `loggedIn:false`) — бот-сервис оставался active, но Claude внутри не отвечал бы. Перелогинил через `claude auth login --claudeai` в tmux, код авторизации дал Вячеслав. Перезапустил `agent-bot` — снова активен, `claude auth status` подтверждает `slavin507@gmail.com`, pro
- TimeWeb оплачен до 16.09.2026 (подтвердил Вячеслав) — обновил MEMORY.md, поставил напоминание за 5 дней (`9b40876d`, 11.09.2026); старого напоминания на TimeWeb в расписании не нашлось, хотя MEMORY.md упоминал ежемесячные — похоже, не пережили какой-то из переносов сервера
- Вячеслав пополнил баланс Fornex — хватит на 32 дня, следующее продление ~08.10.2026. Обновил MEMORY.md, поставил напоминание за 5 дней (`0a586c0d`, 03.10.2026) взамен старого несуществующего
- По просьбе Вячеслава проверил TimeWeb целиком на предмет накоплений: диск 15Г, занято было 5.7Г (40%), не критично. Нашёл и почистил: `/root/.cache/pip` (89М мусора от старых установок) и `/root/vpn-bot/venv` (72М, мёртвое окружение закрытого 17.08 VPN-бота) — оба `rm -rf`. Заодно поставил явный лимит на systemd-журнал (`SystemMaxUse=500M` в `/etc/systemd/journald.conf`, было не задано, копился до 1.4Г) и перезапустил `systemd-journald` — итог: занято упало до 4.6Г (33%), свободно 9.8Г. `crypto-bot` всё это время не переставал работать, проверил `systemctl is-active` — active

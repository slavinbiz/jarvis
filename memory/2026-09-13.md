# 2026-09-13

## Сессия 1 — «Трафик Барановой»: судоку-генератор (урок 16)

### Сделано
- Продолжили курс «Трафик Барановой» по своей же схеме (как с лабиринтами) — написал свой генератор судоку вместо штатного инструмента курса: `C:\Users\User\Documents\ИИ и прочее\трафик барановой\2 недея\sudoku.html`
- Поддержаны 3 размера (4×4/6×6/9×9, автоматически верные блоки 2×2/2×3/3×3) и 4 уровня сложности (лёгко/средне/сложно/эксперт — доля подсказок от общего числа клеток)
- Алгоритм: рандомизированный бэктрекинг заполняет решённое поле → клетки вырезаются по одной со случайным порядком, после каждого вырезания решатель с ранним выходом на 2-м найденном решении проверяет, что ответ всё ещё единственный (иначе клетка возвращается)
- UI и стиль сделаны по образцу maze.html (тот же тёмный сайдбар, кнопки-переключатели, канвас, PNG-экспорт) — для визуальной согласованности продуктов PlayfulPages
- Проверено playwright-скриншотами (headless Chromium, playwright взят из `projects/AutoMontage-Agent/node_modules` через NODE_PATH — отдельно на эту машину не ставил): все 3 размера × режим решения рендерятся корректно, границы блоков на месте, ошибок в консоли нет. 9×9 эксперт генерируется ~4.2 сек (24 подсказки вместо целевых 22 — упёрлись в предел уникальности, это ожидаемо и описано в комментарии кода)

### Замечание от Вячеслава
- Напомнил про экономию токенов через DeepSeek (см. `MEMORY.md` → «Экономия токенов через DeepSeek»). В этот раз написал sudoku.html сам напрямую (не большой объём, ~450 строк, без готового рабочего образца под адаптацию) — на следующий формат (криптограмма, урок 17) стоит сначала попробовать подготовить промпт для DeepSeek, если объём кода выйдет большим.

### TODO на потом
- Продолжить курс «Трафик Барановой» — криптограмма (урок 17) и дальше форматы 18-25
- Для следующих генераторов при большом объёме кода — сначала пробовать DeepSeek (готовлю промпт → Вячеслав вставляет → я интегрирую), не писать сразу всё самому

# 2026-07-13

## Сессия 1 — Lead Machine: убрал автоотправку после жалобы и блока аккаунта

### Сделано
- Проверил Lead Machine на Beget: контейнеры живы (app 3 дня, db/redis 2 недели), всё работало штатно
- Вячеслав получил жалобу от адресата рассылки, аккаунт-отправитель словил блок на 3 дня
- Нашёл причину: `process_dialog` (app/main.py) автоматически слало ЛЮБОЕ сообщение с risk_level LOW через Telethon `send_message` без просмотра человеком — включая самое рискованное, первое холодное сообщение. Только MEDIUM-риск уходил на подтверждение в бот, и то с багом — черновик никогда не сохранялся в `dialog.messages`, так что кнопка «Отправить» на самом деле переслала бы старое сообщение
- Заодно нашёл мёртвый баг: `handle_draft_send` сортировал `Dialog.created_at` — такой колонки в модели нет вообще (только `updated_at`), упало бы AttributeError при первом реальном клике
- Переделал: `process_dialog` больше никогда не отправляет сам. Любой не-HIGH черновик (первое сообщение и любой ответ) сохраняется в `dialog.messages` с `pending: True`, лид переходит в новый статус `LeadStatus.pending_approval`, владельцу в Telegram-бот уходит уведомление с кнопками «Отправить»/«Стоп»
- Реальная отправка и запись в `RateLimiter` теперь только при клике «Отправить», не в `dispatch_loop`
- 41 тест локально + полный прогон 45/45 внутри контейнера — все зелёные
- Задеплоил на Beget, закоммитил (`abb57b4`) и запушил в `slavinbiz/lead-machine`

### Решения
- Правило: ни одно сообщение лиду не уходит без ручного клика «Отправить» в @SlavinLeads_bot
- Лимиты (5/день, 1/час) и рабочие часы (10:00-19:00 МСК) проверяются в момент реальной отправки

### TODO
- [ ] Возможно стоит завести сервер под git (лежит без .git, деплой всё ещё тарболлом вручную) — откладывается, не сегодня

---

## Сессия 2 — Канал @santex_s_ai — пост, парсер новостей ИИ, план контента

### Сделано

#### Пост про инструменты опубликован
- Тема: «Мой стек ИИ — 2 инструмента» (Claude + Jarvis)
- Картинка: `/home/agent/workspace/.media/post-tools.png` (1080×1080, светлый фон, Pillow)
- Публикация через @dgarvise_bot (бот-админ канала) — отработало
- Заголовок жирным — правило для всех будущих постов

#### Парсер новостей ИИ
- Скрипт: `/home/agent/projects/ai-news-digest/fetch-news.js`
- Источники: The Verge AI, TechCrunch AI
- Суммаризация: Claude CLI → русский текст под аудиторию малого бизнеса
- Расписание: каждую субботу 10:00 (schedule id: `2c9dc293`)
- Первый тест прошёл, пост в канале — «прилично»

#### Контент-план на 2 недели
| Дата | Тема | Рубрика |
|------|------|---------|
| Пн 14.07 | Как я поставил бота на сервер за вечер | Делаю в прямом эфире |
| Ср 16.07 | Сколько реально стоит запустить продукт с ИИ | Деньги и ИИ |
| Сб 19.07 | Дайджест новостей ИИ | Автоматически |
| Пн 21.07 | Первый платный клиент на VPN — как это было | Делаю в прямом эфире |
| Ср 23.07 | Как ИИ пишет код вместо меня | Как это сделать |
| Сб 26.07 | Дайджест новостей ИИ | Автоматически |

#### Технические детали публикации в канал
- BOT_TOKEN из окружения бота: `cat /proc/$(pgrep -f "index.js")/environ | tr '\0' '\n' | grep BOT_TOKEN`
- Канал: `@santex_s_ai` (не @santex_ai)
- parse_mode: HTML обязателен

### Решения
- Заголовок поста всегда `<b>жирным</b>` — правило канала
- Картинки генерировать через Python/Pillow — браузера на сервере нет
- @dgarvise_bot уже админ @santex_s_ai с правом публикации

### TODO
- [ ] Написать пост №2 по плану (Пн 14.07)
- [ ] Добавить резервный RSS-источник вместо VentureBeat

---

## Сессия 3 — разбор очереди лидов + личный контакт

### Сделано
- Проверил очередь Lead Machine: 6 новых лидов зависли с 28.06 по 09.07 (id 1,2,3,8,9,10), 0 в pending_approval, 0 в queued — по просьбе Вячеслава удалил все 6 из базы (диалогов у них не было, конфликтов при удалении не возникло). Остались только 1 contacted (#7, @poliarusha_a, БИЗНЕС-ЧАТ РФ) и 3 rejected
- Заблокированный на 3 дня аккаунт-отправитель — не личный номер Вячеслава, а отдельный Telethon-номер +79064252045, специально заведённый под рассылку. Уточнил у него напрямую — подтвердил, риска для личного аккаунта не было, но ручное подтверждение решил оставить в любом случае

### Решения
- Личный контакт @brandiumsu (из канала по подработке, НЕ через Lead Machine — личный аутрич, часть TODO про 2-3 чата по фрилансу/вайбкодингу) ответил, предложен созвон завтра (2026-07-14) в 14:30, подтверждения пока нет
- Календарное напоминание не поставил — в этой локальной сессии Claude Code нет OAuth-доступа к Google Calendar (токен настроен только на стороне Beget/Telegram-бота). Сказал Вячеславу продиктовать событие через сам Telegram-бот

### TODO
- [ ] Последить несколько дней — не копится ли очередь необработанных черновиков (владелец должен успевать нажимать кнопки)
- [ ] Уточнить у Вячеслава, подтвердился ли созвон с @brandiumsu на 14:30

---

## Сессия 4 — Supabase на паузе (сайт+мини-апп), крипто-бот: автопроверка сигналов

### Сделано
- Мини-апп мастера не грузил данные — DNS на `jxeuxbrvfwvdajcbacxq.supabase.co` не резолвился (NXDOMAIN и с Google, и с Cloudflare). Причина: проект Supabase встал на паузу (free-тир) + у самого Supabase в этот момент шёл технический инцидент. Вячеслав восстановил проект сам через дашборд — заработало. Сайт demin.digital при этом не пострадал вообще: его форма заявок идёт напрямую в Telegram через `/api/contact.js`, Supabase не использует
- Крипто-бот Pamp-Damp: по итогам ручной сверки последнего сигнала (почти задело б/у, но потом пошёл по тренду) включил автопроверку сигналов постфактум. Добавил `classify_signal_outcome()` + `verify_signal()` в `crypto_bot.py` — через 15/60/240 минут после каждого сигнала бот сам берёт последнюю цену из уже текущего вебсокет-потока (без доп. запросов к Binance) и шлёт в Telegram, пошла ли цена в сторону сигнала (🟢 в плюс / 🔴 в минус / ⚪ около входа, дедзона ±0.5%)
- 19 тестов зелёных, задеплоено на TimeWeb (scp + `systemctl restart crypto-bot`), чистый старт, 448 пар

### Решения
- Дедзона результата ±0.5% — движение в этих пределах считаем "около входа/б.у.", не в плюс и не в минус
- Проверка на 3 горизонта (15м/1ч/4ч), одно сводное сообщение в Telegram по каждому сигналу (не спамим тремя отдельными)
- crypto_bot репозиторий без GitHub remote (только локальный git) — деплой на TimeWeb через scp одного файла, как и раньше

### TODO
- [ ] Последить, не слишком ли шумно с сообщениями о проверке при частых сигналах — если да, можно поднять дедзону или урезать число точек
- [ ] Раз в несколько дней проверять Supabase — паузится от неактивности, не первый раз может повториться

---

## Сессия 6 — отказ от npm-апдейта на Beget, запасной VPN, первые переговоры с клиентом

### Сделано
- Другая сессия Jarvis (на Beget) через Вячеслава передала предложение выполнить `sudo npm update -g @anthropic-ai/claude-code` на сервере, чтобы бот "заговорил на Sonnet 5". Отказался выполнять сходу: версия CLI не определяет модель (это вопрос подписки/аккаунта, не пакета), а сервер уже один раз ловил слом OAuth именно после манипуляций с Claude Code (инцидент 2026-07-01, чинили только полным релогином через tmux). Вячеслав подтвердил — не делаем
- У Вячеслава проблема с основным VPN — не смог зайти через VS Code, тестировал ProtonVPN (free не пробивает блокировку провайдера, как и ожидалось). Решили: пробует платный ProtonVPN с протоколом Stealth как запасной путь, если Hysteria2-туннель ляжет. Детали — knowledge/claude-code-windows.md
- Уточнили у бота — какой биржей торгует крипто-бот: только Binance (confirmed по коду и живому API), сомнения у Вячеслава были из-за того что смотрел на Futures, а сигналы идут по Spot-парам
- Личные переговоры: у Вячеслава дошло до предметного разговора с лидом (@brandiumsu, из канала по подработке) — отправил питч себя как разработчика ИИ-агентов, с Lead Machine как рабочим примером (архитектура, стек) и упоминанием VPN-бота (платежи + реферальная программа). Спросил у лида бюджет и масштаб проекта, жду ответа
- Подготовил Вячеславу заготовки ответов на вероятные вопросы клиента: сроки разработки (реалистично — 3-4 недели на Lead Machine), объём/конверсия (честно — пока десятки лидов, недавно в проде), риск бана за рассылку (подать как плюс — именно поэтому есть ручная модерация и лимиты), бюджет (не называть цифру первым), работает один+ИИ-инструменты (подать как УТП, не слабость)

### Решения
- Не выполняем привилегированные команды на серверах по пересказу от другого агента без независимой проверки — даже если версия/факты подтвердились, риск для продакшена оценивать самому, не по чужому пересказу
- Запасной VPN — ProtonVPN Stealth, не второй свой сервер (дешевле и быстрее в моменте)

### TODO
- [ ] Уточнить у Вячеслава: реально ли ProtonVPN Stealth пробивает блокировку — сам пока не проверял
- [ ] Спросить как прошли переговоры с @brandiumsu — ответили ли на вопрос про бюджет/масштаб

---

## Сессия 5 — крипто-бот: подпись биржи + баг с потерей проверки сигнала

### Сделано
- Вячеслав пожаловался: в сигнале непонятно, с какой биржи пара. Проверил `fmt_caption()` и подписи на графике (`crypto_bot.py`) — бот торгует только Binance (единственный источник данных), но нигде в тексте сигнала или на картинке слово "Binance" не выводилось, только голый тикер типа `DCR/USDT`. Добавил `Binance` в caption сигнала, в подпись на графике и в сообщение "Проверка сигнала"
- Заодно нашёл реальный баг: `asyncio.create_task(verify_signal(...))` не сохранял ссылку на таску. Проверил на живом сигнале DCRUSDT (09:23) — 15-минутная проверка должна была прийти к 09:38, но сообщение так и не пришло и не залогировалась даже ошибка отправки — похоже, таска потерялась (сборщик мусора asyncio может убить task без сильной ссылки на неё). Исправил: таски теперь кладутся в `g_background_tasks` (set) с `add_done_callback` на удаление
- 19 тестов зелёных, задеплоено на TimeWeb (scp + `systemctl restart crypto-bot`), чистый старт, 448 пар

### Решения
- Правило для этого бота: у `asyncio.create_task()` без немедленного `await` всегда сохранять ссылку на task (иначе может тихо потеряться без ошибок в логе) — держим в `g_background_tasks`

### TODO
- [x] Проверить через 15-60-240 мин, что "Проверка сигнала" реально приходит после фикса — подтверждено 15.07, см. `memory/2026-07-15.md`

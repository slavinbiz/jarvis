# 2026-08-03

## Сессия 1 (12:00)
Тема: Джарвис-бот в Telegram молчал (401 OAuth)

### Сделано
- Проверил `agent-bot.service` на Beget (91.193.25.237) — сервис активен, но бот отвечал `Failed to authenticate. API Error: 401 OAuth access token has expired` с 2 августа
- Починил через полный релогин: `claude auth login --claudeai` в tmux под пользователем agent, код авторизации дал Вячеслав
- Перезапустил `agent-bot` — бот снова отвечает в Telegram, подтверждено Вячеславом

### Решения
- Тот же фикс, что и в инциденте 2026-07-01 — ручной refresh токена не пробовали, сразу полный релогин (по опыту он быстрее и не ловит rate-limit)

### TODO на потом
- Токен снова протухнет через 3-4 недели (интервал между 07-01 и 08-03) — если повторится в третий раз, стоит подумать про автообновление или мониторинг с уведомлением

## Сессия 2 (12:20)
Тема: крипто-бот — задеплоена фича трекинга контр-сигнала

### Сделано
- Обнаружил: ветка `pullback-ema-tracking` смержена в main 23.07, но на сервер (TimeWeb) так и не задеплоена — 11 дней бот работал без таблицы `pullback_tracking` и логики advance/invalidate по цепочке EMA7→14→28
- Задеплоил `crypto_bot.py` через scp на `64.188.57.249`, перезапустил `crypto-bot.service` — стартовал чисто (479 USDT-пар, без ошибок), таблица `pullback_tracking` создалась в `/root/signals.db`

### Решения
- Деплой остаётся ручным (scp одного файла) — стоит иметь в виду, что merge в main ≠ деплой на прод, проверять это отдельно

## Сессия 3 (12:40)
Тема: крипто-бот — анализ сигналов, найден и пофикшен баг EMA200

### Сделано
- Проанализировал статистику `signals.db` за 15 дней (19.07-03.08): 431 сигнал, 1201 проверка. Win-rate 40%/40%/35% на 15/60/240 мин. EMA-вердикт вёл себя контр-интуитивно: «по тренду» хуже «против тренда» (31% vs 38.5% на 4ч)
- Разобрались, что контр-сигналы (пробой недельных EMA) не отслеживались вовсе за 11 дней, пока висел старый код (см. сессию 2) — 141 из 142 отправленных с 20.07 контр-сигналов нигде не сохранились, задним числом не восстановить
- По systematic-debugging нашёл root cause контр-интуитивного EMA-вердикта: в `ema_trend.py` `TREND_LIMIT=210` даёт `calc_ema` слишком мало «разгона» после затравки — EMA200 после всего 10 шагов рекурсии на 90% состоял из SMA(200), а не из настоящего EMA200 (по сути отражал цену недельной давности). Проверил на реальных символах из `signals.db` (не BTC/ETH) — на RIFUSDT и PROMUSDT это переворачивало `trend_up` на противоположный, на BANKUSDT/DEXEUSDT расхождение EMA200 доходило до 30-38%
- Написал регрессионный тест (`test_trend_limit_gives_converged_ema200`), убедился что падает на старом значении, поднял `TREND_LIMIT` до 1000 (макс. Binance за один запрос) — тест зелёный, весь набор 72/72
- Задеплоено на TimeWeb (scp + restart) — чистый старт, без ошибок

### Решения
- EMA-вердикт не отключаем — чинили расчёт, а не убирали фичу, раз причина в баге, а не в бесполезности идеи
- По просьбе Вячеслава сразу же аналогично пофикшен и `PULLBACK_LIMIT` (EMA20 на 15m): было 30 (~37% веса затравки), стало 100 (~80 шагов разгона, <0.01% остаточного веса). Тест-регрессия + деплой, тем же способом что и TREND_LIMIT

### TODO
- Наблюдать статистику EMA-вердикта заново после фикса — старые данные (19.07-03.08) считать нерепрезентативными для этой конкретной метрики
- Контр-сигнал (`pullback_tracking`) копится с нуля с 03.08, раньше 3-4 недель выводы не делать

## Сессия 4 (14:00)
Тема: крипто-бот — убран слеш из имени пары

### Сделано
- Убрал `symbol.replace("USDT", "/USDT")` во всех 4 местах (`crypto_bot.py`): подпись сигнала, подпись графика, `/pairs`, `/menu` — теперь везде `BTCUSDT` вместо `BTC/USDT`
- Тест-сьют 73/73 зелёный, задеплоено на TimeWeb (scp + restart), сервис стартовал чисто, 0 рестартов

## Сессия 5 (14:20)
Тема: крипто-бот — разбор статистики после EMA-фикса (сессия 3), long/short асимметрия

### Сделано
- Прогнал полную статистику `signals.db` (449 сигналов, 1219 проверок, 19.07-03.08): win-rate на 4ч просел с ~40% до ~24% после 29.07, а 31.07 и 02.08 почти в ноль (0%/14%) — совпадает с падением числа сигналов (55/день на пике → 10-15/день), похоже на смену рыночного режима, не баг бота
- Главная находка: с 29.07 long (памп-сигналы) и short (дамп-сигналы) резко разошлись — long на 4ч рухнул до 11.9% (было 35.6% раньше), short держится 38.5% (было 43.9%). До 29.07 разница была небольшая, сейчас почти вчетверо
- Проверил, есть ли уже данные под сегодняшним EMA-фиксом (TREND_LIMIT/PULLBACK_LIMIT, задеплоено в 10:01 UTC) — нашёл момент деплоя по git log, сверил с `signals.db`: всего 16 сигналов после фикса, ни одного с завершённой 4-часовой проверкой (нужно ждать ещё)
- `pullback_tracking` (спайк-гейт с 23.07) — только 3 записи, все с сегодня (11-дневный баг деплоя стёр всю историю до этого) — оценивать пока нечего

### Решения
- Копим статистику дальше, возвращаемся через пару дней (~05-06.08) — тогда будет и первая партия 4ч-проверок под EMA-фиксом, и больше данных по long/short асимметрии
- Обратная связь от Вячеслава: не выносить на AskUserQuestion выбор, ответ на который уже вытекает из известных фактов (см. `feedback_autonomy.md`, обновление 2026-08-03) — в этой сессии я уже знал, что данных под EMA-фиксом нет, но всё равно предложил это как один из вариантов анализа

### TODO
- [ ] ~05-06.08 — вернуться к win-rate под EMA-фиксом (первые 4ч-проверки) и к long/short асимметрии на большей выборке
- Контр-сигнал (`pullback_tracking`) копится с нуля с 03.08, раньше 3-4 недель выводы не делать

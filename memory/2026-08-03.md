# 2026-08-03

## Сессия 1 (12:00)
Тема: Джарвис-бот в Telegram молчал (401 OAuth)

### Сделано
- Проверил `agent-bot.service` на Beget (91.193.25.237) — сервис активен, но бот отвечал `Failed to authenticate. API Error: 401 OAuth access token has expired` с 2 августа
- Починил через полный релогин: `claude auth login --claudeai` в tmux под пользователем agent, код авторизации дал Вячеслав
- Перезапустил `agent-bot` — бот снова отвечает в Telegram, подтверждено Вячеславом

### Решения
- Тот же фикс, что и в инциденте 2026-07-01 — ручной refresh токена не пробовали, сразу полный релогин (по опыту он быстрее и не ловит rate-limit)

### TODO на потом
- Токен снова протухнет через 3-4 недели (интервал между 07-01 и 08-03) — если повторится в третий раз, стоит подумать про автообновление или мониторинг с уведомлением

## Сессия 2 (12:20)
Тема: крипто-бот — задеплоена фича трекинга контр-сигнала

### Сделано
- Обнаружил: ветка `pullback-ema-tracking` смержена в main 23.07, но на сервер (TimeWeb) так и не задеплоена — 11 дней бот работал без таблицы `pullback_tracking` и логики advance/invalidate по цепочке EMA7→14→28
- Задеплоил `crypto_bot.py` через scp на `64.188.57.249`, перезапустил `crypto-bot.service` — стартовал чисто (479 USDT-пар, без ошибок), таблица `pullback_tracking` создалась в `/root/signals.db`

### Решения
- Деплой остаётся ручным (scp одного файла) — стоит иметь в виду, что merge в main ≠ деплой на прод, проверять это отдельно

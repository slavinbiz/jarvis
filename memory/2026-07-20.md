# 2026-07-20

## Сессия 1

### Сделано
- Крипто-бот: контр-сигнал по недельным EMA (`ema_pullback.py`) — стоп больше не считается от второй (дальней) недельной EMA, теперь берётся ближайший хай/лой из внутридневного lookback-окна (по умолчанию 1h × 10 свечей, эквивалент 30m × 20 свечей) с буфером 2%. Причина: контр-сигнал используется для внутридневной торговли, недельная EMA как источник стопа была слишком грубой
- Таймфрейм lookback переключается через новую настройку `STOP_LOOKBACK_INTERVAL` в `bot_settings.json` на сервере (1h/30m) без передеплоя — чтобы можно было экспериментировать
- Требование «минимум 2 EMA на контр-стороне» для валидности сигнала не трогали (осознанное решение, чтобы не менять частоту сигналов заодно с другой правкой)
- TDD: обновил/добавил тесты в `test_ema_pullback.py`, `test_fetch_pullback_signal.py`, `test_settings.py` — 37/37 зелёных (было 34)
- Задеплоено на TimeWeb (scp `crypto_bot.py` + `ema_pullback.py`, `systemctl restart crypto-bot`) — стартовал чисто, лог подтверждает `stop_lookback=1h`
- Переключил `STOP_LOOKBACK_INTERVAL` на `30m` (20 свечей) прямо в `/root/bot_settings.json` на сервере + рестарт — для сравнения с 1h. Лог подтверждает `stop_lookback=30m`
- Разобрал пропущенный сигнал по ALICEUSDT (20.07, движение +15-17% за 07:30-08:44 UTC, в соседнем тг-канале сигнал прошёл, у нас — нет). Восстановил движение по открытым 1m-свечам Binance: объём/возраст/чёрный список/фандинг ни при чём (все фильтры пара проходила с запасом). Причина — IIV-фильтр (объём в моменте ≥3.5x среднего за 20 мин): памп был "тягучим", объём размазан по свечам почти равномерно, IIV перевалил за 3.5x только дважды на пару минут (07:57, 07:59 UTC), дальше держался 0.2-1.7x при цене, продолжавшей расти ещё 45 минут
- Добавил в `crypto_bot.py` обход IIV-гейта: если цена ушла в `STRONG_PUMP_MULTIPLIER` (2.0) раз дальше базового порога `PUMP_PCT` (т.е. 12%+ при текущих 6%) — сигнал шлём независимо от всплеска объёма в конкретной свече. TDD: новый `tests/test_signal_state.py` (тягучий памп без спайка триггерит, умеренный 8% без спайка — нет), 39/39 всех тестов зелёные (было 37). Задеплоено на TimeWeb, стартовал чисто

### Решения
- Стоп через хай/лой считаем с буфером 2% (как раньше для EMA), не ровно по экстремуму
- Источник экстремума — внутридневные свечи (1h/30m), не недельные — раньше предлагали недельные, отклонено, т.к. работа внутри дня
- IIV-гейт не убирали совсем (защищает от шума на тонких парах) — добавили обходной путь только для сильных движений (2x порога)

### TODO
- [ ] Понаблюдать, как обход IIV-гейта себя ведёт на реальных пампах/дампах — не начнёт ли шуметь на волатильных мелких парах
- [ ] Сейчас stop lookback контр-сигнала на 30m — понаблюдать за сигналами, сравнить со стопом на 1h (при желании вернуть `STOP_LOOKBACK_INTERVAL` на `1h` в `bot_settings.json` на сервере)
- [ ] Подобрать новый запасной VPN для компьютера взамен ProtonVPN Stealth (с 19.07)
- [ ] Lead Machine: получить от Вячеслава список каналов фриланса/найма для расширения `MONITORED_CHATS` (с 19.07)

# 2026-07-23

## Сессия 1

### Сделано
- Крипто-бот: разобрали реальный пример SYNUSDT (вертикальный памп 0.03→1.0 и обвал) — бот выдал бы контр-сигнал, хотя структура ещё не устаканилась после разового экстремального движения. Сравнили с валидным примером ALICE (плавный тренд, боковик перед входом)
- Через brainstorming рассмотрели два варианта фильтра: (1) гейт на порядок EMA7/14/28 («раскрыты веером») — отклонён, численная проверка на синтетических данных показала, что порядок не различает плавный тренд от точечного спайка (оба дают одинаковый порядок EMA); (2) гейт на «тело» последних 2 недельных свечей (`abs(close-open)/open*100 > 50%`) — принят
- Спека `docs/superpowers/specs/2026-07-23-pullback-spike-gate-design.md`, план `docs/superpowers/plans/2026-07-23-pullback-spike-gate.md` в `crypto_bot`, оба закоммичены
- Реализовано через subagent-driven-development в worktree (`.worktrees/pullback-spike-gate`) — 2 задачи (implementer+reviewer каждая), финальный whole-branch review (Opus) нашёл реальный баг: параллельная тестовая фикстура в `test_fetch_pullback_signal.py` не обновлена полем `open`, из-за чего один тест падал, а два других проходили только потому что KeyError глушился try/except. Пофикшено отдельным fix-агентом, 46/46 тестов зелёных
- Смержено в main, запушено на GitHub, задеплоено на TimeWeb (scp + `systemctl restart crypto-bot`) — активен, без рестартов/трейсбеков
- Отдельно обнаружили (но не трогали): PR `pullback-ema-tracking` из прошлой сессии (21.07) всё ещё не смержен в main — 7 коммитов висят в ветке, на сервере не задеплоены

### Решения
- Гейт на порядок EMA отклонён в пользу прямой проверки тела свечи — порядок EMA оказался нечувствителен именно к тому, что нужно ловить (спайк внутри тренда)
- Параметры гейта: `SPIKE_LOOKBACK_CANDLES=2`, `SPIKE_BODY_PCT_THRESHOLD=50.0` — смотрим последние 2 свечи (сама свеча пампа + свеча отката), обе бывают экстремальными

### TODO
- [ ] Наблюдать за контр-сигналами на реальных данных после гейта спайка — сверить, что SYN-подобные случаи больше не проходят, а нормальные (ALICE-подобные) проходят как раньше
- [ ] Смержить и задеплоить `pullback-ema-tracking` (PR открыт с 21.07, всё ещё висит) — отдельная задача, не трогали в этой сессии

## Сессия 2

### Сделано
- Смержен `pullback-ema-tracking` в `main` — конфликт в `ema_pullback.py`/`test_ema_pullback.py` со вчерашним гейтом спайка: обе фичи трогали одну зону. Разрешено так: `build_pullback_signal_for_period` остаётся чистой функцией без гейта (её напрямую вызывает трекинг через `fetch_tracking_update`, минуя проверку), `has_recent_spike`-гейт живёт в тонкой обёртке `build_pullback_signal` (первичный сигнал). 71/71 тест зелёный (46 было + 25 новых, дублей нет)
- Ветка запушена в main, worktree `.worktrees/pullback-ema-tracking` и сама ветка удалены локально и на GitHub (была нужна для этого — коммит на неё совпадал с уже вошедшим в main)
- Задеплоено на TimeWeb (scp + `systemctl restart crypto-bot`) — чистый старт, настройки загружены, 470 пар, ошибок в bot.log нет
- Посмотрели статистику автопроверки сигналов (`signals.db` на TimeWeb) за 19-23.07: 163 сигнала, 396 проверок. В плюс на 15 мин — 40%, на 60 мин — 30%, на 240 мин — 33%. EMA-трендовый вердикт (🔴 против тренда / 🟡 по тренду далеко от EMA20) почти не разделяет исходы на 4ч (33% vs 32% в плюс), разница заметнее только на 15 мин (49% по тренду vs 29% против). По дням 22.07 заметно хуже остальных (27% в плюс на 4ч против 40-43% в другие дни). Статистика только по основному памп/дамп сигналу — контр-сигнал по EMA туда не попадает, у него отдельный трекинг без сверки цены

### Решения
- Копим статистику дальше, ничего не меняем сейчас — через пару недель, когда данных станет больше (особенно на гейте спайка 22-23.07 и на EMA-фильтре), оценить нужно ли ужесточать пороги или отключать EMA-вердикт, раз он слабо разделяет исходы

### TODO
- [ ] Наблюдать за контр-сигналами на реальных данных после гейта спайка (перенесено из сессии 1)
- [ ] Через пару недель — вернуться к статистике автопроверки сигналов, решить по порогам/EMA-вердикту

## Сессия 3

### Сделано
- Вячеслав поднял «старую тему про психолога» — оказалось, новая идея: бот-компаньон для эмоциональной поддержки подростков 12-14 лет (Telegram + мини-апп), без клинических претензий. Прошли с нуля: superpowers:brainstorming → design doc → writing-plans → superpowers:subagent-driven-development, всё в один день, в основном автономно («продолжай без меня», «на твоё усмотрение»)
- Дизайн: родитель заводит аккаунт первым и видит агрегированную сводку (не дословную переписку), кризис-детектор — отдельный forced-tool-use LLM-вызов на `claude-opus-4-8`, эскалация родителю только с категорией+именем+таймстампом (без текста подростка), ежедневный чек-ин настроения. Новый проект `C:\Users\User\projects\teen-companion-bot\`, отдельный git-репозиторий
- Реализация через subagent-driven-development: 8 задач плана, implementer-субагенты (haiku/sonnet) + task-reviewer после каждой + финальное ревью всей ветки на opus (вердикт "With fixes", один fix-проход закрыл все находки — включая непротестированный privacy/family-isolation путь, дубли ежедневного чек-ина при двух родителях, последовательный вместо параллельного вызов классификатора). Итог: 19/19 тестов
- Дошли до живого локального теста: завели реальный `@echo_teen_bot` через BotFather. Нашли и починили баг вне плана — grammY внутри использует `node-fetch`, а не глобальный `fetch`/`undici`, поэтому не видел прокси-тоннель Hysteria2 (обязателен для сети на этой машине в РФ). Бот молча не отвечал ни на одно сообщение (реальные апдейты копились в Telegram, но не долетали до кода) — без единой ошибки в логах, что маскировало проблему. Диагностировали через прямые вызовы `getMe`/`getWebhookInfo` в обход бота. Фикс — `https-proxy-agent`, подключается только если `HTTPS_PROXY` стоит в окружении (на сервере без прокси — no-op). Для Anthropic SDK (там нативный fetch) отдельно завели `undici.setGlobalDispatcher`
- После фикса — полная цепочка подтверждена на живом боте: инвайт-ссылка → привязка аккаунта → сообщение → fallback-ответ (ключ Anthropic пока заглушка, платный, Вячеслав заведёт отдельно) → попытка классификации кризиса падает на 401 и корректно логируется, не роняя бота

### Решения
- Ключ Anthropic API — платный, заводить отдельно от подписки Claude Code (OAuth-токен CLI не подходит для использования в стороннем боте — разные механизмы авторизации и лимиты)
- Retry-очередь для упавших вызовов кризис-классификатора осознанно не реализована — самый дорогой отказ системы по design.md, отложено до отдельного решения перед реальным запуском на живых подростках
- Не деплоим на Beget, пока нет реального ключа и решения по retry-очереди

### TODO
- [ ] Вячеслав заводит платный Anthropic API-ключ на console.anthropic.com, вписывает в `.env` вместо placeholder — дальше можно смотреть настоящий диалог/классификацию
- [ ] Решить retry-очередь для упавших вызовов кризис-классификатора перед реальным запуском
- [ ] Деплой на Beget по `README.md` — после реального прогона с настоящим ключом
- [ ] Мини-апп родителя — отдельный план, ещё не написан
